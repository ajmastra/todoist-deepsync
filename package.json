{
	"name": "todoist-subtask-sync",
	"version": "1.0.0",
	"description": "Obsidian plugin: Todoist DeepSync - sync Todoist tasks with full subtask support",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"devDependencies": {
		"@types/node": "^20.10.0",
		"esbuild": "^0.19.0",
		"typescript": "^5.3.0"
	},
	"dependencies": {
		"obsidian": "latest"
	}
}
